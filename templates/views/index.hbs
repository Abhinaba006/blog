{{>header}}

<body>
    {{>nav}}
    <script src="/js/index.js"></script>

    <link rel="stylesheet" href="/css/master.css">

    <div class="btn btn-link"><a href="/post">
            <h1>New Post 😋</h1>
        </a></div>

    <br>

    {{#each blogs}}
    <div class="container jumbotron">
        <h2><a href="blogs/{{this._id}}">{{this.title}}</a></h2>
        <span>{{this.text}}</span>
        {{#ifEquals ../userid this.owner }}
        <button id="delete-blog" onclick="deleteBlog('{{this._id}}')">delete</button>
        <a href="/editpost/{{this._id}}"><button>Edit</button></a>
        {{/ifEquals}}
        <br>
        Created at {{{trimDate this.createdAt}}}
        <br>
        <i>Written by {{this.author}}</i>
    </div>
    {{/each}}
</body>